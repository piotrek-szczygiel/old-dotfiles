#!/usr/bin/env bash

i3-msg -t get_workspaces | grep "2:term" > /dev/null 2>&1
status=$?
i3-msg "workspace 2:term" > /dev/null 2>&1

if [ $status -ne 0 ]; then
    termite -d $HOME -e "fish -c \"neofetch; and fish\"" &
fi
