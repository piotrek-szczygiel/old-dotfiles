- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.config/nvim/init.vim: init.vim
    ~/.gitconfig: gitconfig
    ~/.local/share/konsole/Dracula.colorscheme: Dracula.colorscheme
    ~/.zshrc: zshrc
