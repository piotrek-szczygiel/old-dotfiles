- defaults:
    link:
        create: true
        relink: true

- clean:
    ~
    ~/.config

- link:
    ~/.config/fish: fish
    ~/.config/i3: i3
    ~/.config/nvim: neovim
    ~/.config/omf: omf
    ~/.config/termite: termite
    ~/.gitconfig: gitconfig
    ~/.xprofile: xprofile
