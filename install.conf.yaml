- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.config/nvim/init.vim: init.vim
    ~/.gitconfig: gitconfig
    ~/.hyper.js: hyper.js
    ~/.tmux.conf: tmux.conf
    ~/.Xresources: Xresources
    ~/.zshrc: zshrc

- shell:
    - xrdb ~/.Xresources
