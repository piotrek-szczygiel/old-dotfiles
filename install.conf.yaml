- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.config/nvim/init.vim: init.vim
    ~/.gitconfig: gitconfig
    ~/.spacemacs: spacemacs.el
    ~/.zshrc: zshrc.zsh
