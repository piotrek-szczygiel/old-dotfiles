set -xg EDITOR nvim
set -xg PATH $HOME/.dotfiles/bin $PATH

alias ls "better_ls"

alias ga "git add"
alias gc "git commit"
alias gca "git commit -a"
alias gco "git checkout --"
alias gd "git diff"
alias gl "git pull"
alias gp "git push"
alias gs "git status"

alias o "xdg-open"

alias q "exit"
