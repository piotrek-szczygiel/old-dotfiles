set -xg EDITOR nvim
set -xg PATH $HOME/.dotfiles/bin $PATH

alias ga "git add"
alias gc "git commit"
alias gca "git commit -a"
alias gd "git diff"
alias gl "git pull"
alias gp "git push"
alias gs "git status"

alias o "xdg-open"

alias q "exit"
